import React from "react";
import ChatWrapper from "@/components/dashboard/chat/ChatWrapper";

const Chat = () => {
  return (
    <section>
      <ChatWrapper />
    </section>
  );
};

export default Chat;
